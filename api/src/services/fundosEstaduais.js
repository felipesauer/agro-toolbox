import { round } from '../utils/formatters.js';

// Stub — será implementado completamente na Fase 4
export function calcularFundosEstaduais({ estado, produto, quantidade, valorOperacao }) {
  return {
    estado,
    produto,
    quantidade,
    valorOperacao,
    fundo: 'Em desenvolvimento',
    aliquota: 0,
    valorFundo: 0,
    mensagem: 'Funcionalidade será implementada na Fase 4',
  };
}
