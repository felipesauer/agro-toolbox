// Stub — será implementado completamente na Fase 4
export function consultarCfop({ tipo, destino, operacao, produto }) {
  return {
    tipo,
    destino,
    operacao,
    produto: produto || null,
    cfop: null,
    descricao: 'Funcionalidade será implementada na Fase 4',
    mensagem: 'Assistente CFOP em desenvolvimento',
  };
}
